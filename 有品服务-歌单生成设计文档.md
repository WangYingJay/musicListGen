# 桌面端生图 App 最终设计文档

## 1. 项目定位

### 1.1 产品名称

暂定名：`有品服务-歌单生成`

### 1.2 产品目标

将当前网页版“歌单图片生成工作台”升级为桌面端生图 App，核心解决两类问题：

1. 上游生图耗时可能超过 300s，不能依赖一次 HTTP 长连接等待结果。
2. 用户需要更稳定的桌面体验，包括任务队列、历史画廊、本地保存、参数预设、图生图和配置管理。

最终产品应支持：

- 文生图
- 图生图 / 图片编辑
- 异步任务提交与轮询
- 本地画廊与历史记录
- API 配置和服务端默认 Key
- 代理设置
- 后续扩展 ControlNet、批量生成、插件后端

### 1.3 当前基础

当前项目已经具备：

- 静态前端：`index.html`
- Python 后端：`FastAPI`
- 异步任务接口：
  - `POST /api/images/generations`
  - `POST /api/images/edits`
  - `GET /api/tasks/{taskId}`
  - `GET /api/tasks/{taskId}/image`
- 后端任务状态：
  - `pending`
  - `running`
  - `succeeded`
  - `failed`
- 本地任务库：`SQLite`
- 本地文件结果保存：`data/results`

桌面端 App 不重新设计后端主链路，而是复用当前异步任务模式。

## 2. 总体结论

### 2.1 推荐架构

采用：

```text
Electron 主进程
  |
  | 启动 / 管理 Python FastAPI Sidecar
  | 管理窗口、菜单、代理、文件保存、系统通知
  v
React 渲染进程
  |
  | HTTP 调用 localhost 后端
  v
Python FastAPI 后端
  |
  | 异步任务提交、轮询、上传文件、结果落盘
  v
上游 OpenAI 兼容图片接口 / 其他生图后端
```

### 2.2 核心设计判断

1. 桌面端不直接调用上游生图接口，统一走本地 FastAPI 后端。
2. Python 后端继续负责异步任务、上传文件、结果落盘和上游适配。
3. Electron 主进程负责桌面能力：窗口、托盘、代理、本地文件保存、服务启动。
4. React 渲染层只负责交互、状态展示和任务队列，不保存明文 Key。
5. 生图请求永远采用“提交任务 -> 获得 task_id -> 轮询结果”的模式，避免 300s 断连。

## 3. 技术栈

### 3.1 桌面端

```json
{
  "runtime": "Electron",
  "frontend": "React 18 + TypeScript",
  "build": "Vite",
  "style": "TailwindCSS",
  "ui": "Radix UI",
  "state": "Zustand",
  "http": "Axios",
  "drag": "react-dnd",
  "icons": "lucide-react",
  "storage": "SQLite / IndexedDB / electron-store",
  "secret": "keytar 优先，electron-store encrypted fallback"
}
```

### 3.2 后端

```json
{
  "framework": "FastAPI",
  "server": "uvicorn",
  "httpClient": "httpx",
  "taskStore": "SQLite",
  "fileStore": "local filesystem",
  "futureQueue": "Redis / Celery 可选"
}
```

### 3.3 打包建议

MVP 阶段：

- Electron App 启动时自动拉起本地 Python 后端。
- 后端监听 `127.0.0.1` 随机可用端口。
- Electron 主进程通过 IPC 把后端地址传给 React。

后续正式打包：

- 使用 `electron-builder`
- Python 后端作为 sidecar 可执行文件随 App 分发
- macOS / Windows 分别做平台包

## 4. 信息架构

### 4.1 顶层导航

```text
文生图
图生图
画廊
任务
设置
```

### 4.2 页面结构

主界面采用三栏式：

```text
+------------------------------------------------------------------+
| Logo | 文生图 | 图生图 | 设置                         用户状态     |
+-------------+-----------------------------------+----------------+
|             |                                   |                |
| 左侧画廊     | 主生图工作区                       | 右侧配置面板    |
| 历史缩略图   | 提示词 / 参数快捷栏 / 生成按钮        | API / 模型参数  |
| 筛选 / 排序  | 当前任务反馈 / 结果预览              | 代理 / 预设     |
|             |                                   |                |
+-------------+-----------------------------------+----------------+
| 在线状态 | API 类型 | 余额 | 后端状态 | 任务队列数量              |
+------------------------------------------------------------------+
```

## 5. UI 设计规范

### 5.1 视觉风格

关键词：

- 深色主题
- 霓虹蓝紫点缀
- 现代扁平
- 轻微玻璃质感
- 高密度但不拥挤
- 工具型 App，而不是营销页

### 5.2 主题变量

```css
:root {
  --bg-primary: #0a0a0f;
  --bg-secondary: #111118;
  --bg-tertiary: #1a1a24;
  --border-color: #2a2a35;
  --accent-primary: #3b82f6;
  --accent-secondary: #8b5cf6;
  --text-primary: #e5e7eb;
  --text-secondary: #9ca3af;
  --success: #10b981;
  --warning: #f59e0b;
  --error: #ef4444;
}
```

### 5.3 布局尺寸

```text
顶部栏高度：40px
底部状态栏高度：32px
左侧画廊宽度：260px，可折叠到 64px
右侧配置面板宽度：320px，可折叠到 72px
主工作区最小宽度：640px
窗口最小尺寸：1280 x 760
推荐尺寸：1440 x 900
```

### 5.4 核心组件

#### 提示词输入框

- 背景：`#1a1a24`
- 边框：`#2a2a35`
- 聚焦边框：`#3b82f6`
- 聚焦光晕：`0 0 0 2px rgba(59, 130, 246, 0.2)`
- 高度：最小 220px，可拖拽调整
- 支持多行输入
- 支持 `/` 快捷词弹窗
- 支持历史提示词下拉
- 支持敏感词检测结果内联提示

#### 生成按钮

- 大号主按钮
- 背景：`linear-gradient(135deg, #3b82f6, #8b5cf6)`
- Hover：亮度提升，轻微放大
- Active：`scale(0.98)`
- Loading：显示旋转状态和“生成中...”
- Disabled：降低透明度并显示原因 Tooltip

#### 画廊卡片

- 背景：`#111118`
- 圆角：`8px`
- 图片比例：默认 `1:1`，支持 `3:4`、`4:3`、`16:9`
- Hover：
  - 边框变为主色
  - 底部浮出操作栏
- 操作：
  - 保存
  - 复制提示词
  - 重新生成
  - 查看元数据
  - 删除记录

#### 任务卡片

- 位置：右下角浮动，也可切换为固定任务面板
- 背景：`rgba(26, 26, 36, 0.88)`
- 效果：`backdrop-filter: blur(10px)`
- 内容：
  - 缩略提示词
  - 当前状态
  - 进度条
  - 已等待时间
  - 操作按钮：取消、重试、收起

## 6. 功能设计

### 6.1 API 配置面板

位置：右侧边栏顶部，折叠卡片。

字段：

- `API Base URL`
  - 默认从后端配置读取
  - 本地开发默认：`http://127.0.0.1:{port}/api`
- `API Key`
  - 可选
  - 当服务端已有 `UPSTREAM_API_KEY` 时，显示“使用服务端默认 Key”
- `使用服务端默认 Key`
  - 开启后清空本地 Key
  - 后续请求不带 Authorization
- `代理设置`
  - 不使用代理
  - HTTP 代理
  - SOCKS5 代理
  - 系统代理
- `连接测试`
  - 优先调用 `/health`
  - 后续可扩展 `/api/preflight`

状态：

- 未配置
- 连接中
- 在线
- 离线
- Key 无效
- 后端不可用

### 6.2 文生图工作区

主区域包括：

- 正向提示词
- 反向提示词
- 风格快捷词
- 参数快捷栏
- 生成按钮
- 结果预览

提示词能力：

- 多行输入
- 历史记录
- 收藏提示词
- `/` 快捷插入
- 关键词着色
- 敏感词检测

快捷词分类：

- 风格：电影感、赛博、胶片、极简、二次元、写实
- 画质：high detail、sharp focus、studio lighting
- 构图：close-up、wide shot、center composition
- 材质：metal、glass、paper texture、fabric
- 歌单场景：夜晚城市、雨夜地铁、黑胶休息室、卧室耳机

### 6.3 参数面板

参数位置：右侧配置面板的“模型参数”卡片。

字段：

- 模型
  - 从 `/api/models` 获取
  - 如果接口不存在，使用本地默认模型列表
- 图片尺寸
  - `1024x1024`
  - `1024x1536`
  - `1536x1024`
  - 自定义宽高
- 采样步数
  - 默认：20
  - 范围：1-80
- CFG Scale
  - 默认：7
  - 范围：1-20
- Seed
  - 随机
  - 手动输入
  - 锁定上一次
- 生成数量
  - 1-4
- 质量
  - auto
  - standard
  - high
- 预计消耗
  - MVP 阶段使用静态估算
  - 后续从后端返回真实价格或余额

### 6.4 任务队列

任务创建后立即进入本地任务队列。

任务状态：

```ts
type TaskStatus =
  | "pending"
  | "running"
  | "succeeded"
  | "failed"
  | "cancelled";
```

任务信息：

- `taskId`
- `prompt`
- `negativePrompt`
- `params`
- `status`
- `progress`
- `message`
- `imageUrl`
- `error`
- `createdAt`
- `startedAt`
- `completedAt`

轮询策略：

- 默认每 2 秒轮询一次
- 窗口失焦后降频到 5 秒
- App 最小化后降频到 10 秒
- 任务完成或失败后停止轮询
- 网络错误时指数退避，最大 30 秒

取消策略：

- MVP：前端取消只停止本地轮询，后端任务继续跑
- V1：新增 `POST /api/tasks/{taskId}/cancel`
- V1：后端支持可取消任务状态

重试策略：

- 保留原始参数
- 创建新任务
- 新旧任务建立 `retryFromTaskId` 关联

### 6.5 历史画廊

位置：左侧栏。

功能：

- 2 / 3 / 4 列切换
- 最近生成优先
- 按模型筛选
- 按尺寸筛选
- 按日期筛选
- 搜索提示词
- 本地保存图片
- 查看元数据
- 复制提示词
- 重新生成

画廊记录结构：

```ts
interface GalleryItem {
  id: string;
  taskId: string;
  imagePath: string;
  thumbnailPath: string;
  prompt: string;
  negativePrompt?: string;
  model: string;
  size: string;
  seed?: number;
  steps?: number;
  cfgScale?: number;
  createdAt: string;
  metadata: Record<string, unknown>;
}
```

### 6.6 图生图 / 编辑

入口：顶部导航“图生图”。

MVP 支持：

- 拖拽图片进入工作区
- 上传参考图
- 设置重绘强度
- 使用同一套任务队列
- 调用当前后端 `POST /api/images/edits`

V1 支持：

- 图片裁剪
- 蒙版重绘
- 多参考图
- 图片权重

V1.5 支持：

- ControlNet
- 姿态图
- 边缘图
- 深度图
- 局部重绘历史

## 7. 后端接口设计

### 7.1 健康检查

```http
GET /health
```

响应：

```json
{
  "status": "ok"
}
```

### 7.2 创建文生图任务

当前已有：

```http
POST /api/images/generations
```

请求：

```json
{
  "model": "gpt-image-2",
  "prompt": "生成一张歌单海报",
  "size": "1024x1024",
  "n": 1,
  "quality": "auto"
}
```

响应：

```json
{
  "id": "task_id",
  "task_id": "task_id",
  "status": "pending",
  "progress": 0,
  "message": "任务已创建，等待后台执行",
  "poll_url": "/api/tasks/task_id",
  "created_at": "2026-04-29T00:00:00+00:00"
}
```

### 7.3 创建图生图任务

当前已有：

```http
POST /api/images/edits
Content-Type: multipart/form-data
```

字段：

- `model`
- `prompt`
- `size`
- `quality`
- `image`

响应同文生图任务。

### 7.4 查询任务

当前已有：

```http
GET /api/tasks/{taskId}
```

响应：

```json
{
  "id": "task_id",
  "task_id": "task_id",
  "kind": "generations",
  "status": "succeeded",
  "progress": 100,
  "message": "图片生成完成",
  "error": null,
  "result": {
    "data": [
      {
        "url": "/api/tasks/task_id/image"
      }
    ]
  },
  "poll_url": "/api/tasks/task_id",
  "created_at": "2026-04-29T00:00:00+00:00",
  "started_at": "2026-04-29T00:00:01+00:00",
  "completed_at": "2026-04-29T00:02:30+00:00"
}
```

### 7.5 获取图片

当前已有：

```http
GET /api/tasks/{taskId}/image
```

返回：`image/png`

### 7.6 建议新增接口

#### 获取后端配置能力

```http
GET /api/capabilities
```

响应：

```json
{
  "server_key_configured": true,
  "supports_generations": true,
  "supports_edits": true,
  "supports_cancel": false,
  "supports_progress": true,
  "supports_moderation": false,
  "supports_models": false,
  "default_api_base": "https://harin.52pick.com/v1"
}
```

#### 模型列表

```http
GET /api/models
```

响应：

```json
{
  "data": [
    {
      "id": "gpt-image-2",
      "name": "GPT Image 2",
      "supports_edit": true,
      "sizes": ["1024x1024", "1024x1536", "1536x1024"]
    }
  ]
}
```

#### 预检

```http
POST /api/preflight
```

用于测试：

- 后端可用
- 上游 Base URL 可访问
- Key 是否有效
- 代理是否可用

#### 敏感词检测

```http
POST /api/moderation/check
```

请求：

```json
{
  "prompt": "..."
}
```

响应：

```json
{
  "ok": true,
  "hits": []
}
```

#### 取消任务

```http
POST /api/tasks/{taskId}/cancel
```

MVP 可先不做，V1 加入。

## 8. 前端 API 适配层

### 8.1 目录建议

```text
desktop/
  electron/
    main.ts
    preload.ts
    python-service.ts
    ipc.ts
  src/
    app/
      App.tsx
      routes.tsx
    api/
      client.ts
      image.ts
      tasks.ts
      capabilities.ts
    stores/
      configStore.ts
      taskStore.ts
      galleryStore.ts
    components/
      layout/
      prompt/
      params/
      gallery/
      tasks/
      settings/
    styles/
      globals.css
```

### 8.2 生图流程

```ts
async function generateImage(input: GenerateInput) {
  const task = await imageApi.createGeneration(input);

  taskStore.addTask({
    taskId: task.task_id,
    prompt: input.prompt,
    params: input,
    status: task.status,
    progress: task.progress ?? 0,
    createdAt: task.created_at,
  });

  const result = await taskApi.pollTask(task.task_id, {
    intervalMs: 2000,
    onUpdate: (latest) => {
      taskStore.updateTask(task.task_id, latest);
    },
  });

  galleryStore.addFromTask(result);
  return result;
}
```

### 8.3 API Key 逻辑

```ts
function buildAuthHeaders(config: ApiConfig) {
  if (config.useServerKey) {
    return {};
  }

  if (!config.apiKey) {
    return {};
  }

  return {
    Authorization: `Bearer ${config.apiKey}`,
  };
}
```

规则：

- `useServerKey = true` 时，前端不发送 Key。
- `useServerKey = false` 且用户填写 Key 时，发送 `Authorization`。
- Key 不写入日志。
- Key 不进入任务元数据。
- Key 优先存在系统钥匙串，失败时再用本地加密存储。

## 9. Electron 主进程设计

### 9.1 主职责

Electron 主进程负责：

- 创建窗口
- 启动 Python 后端
- 分配本地端口
- 监听后端健康状态
- 设置代理
- 文件保存对话框
- 系统通知
- 应用菜单
- 托盘图标

### 9.2 Python Sidecar 启动流程

```text
App 启动
  |
  | 查找可用端口
  v
启动 Python 后端
  |
  | 轮询 /health
  v
后端就绪
  |
  | 通过 IPC 通知渲染进程
  v
React 初始化 API Client
```

### 9.3 后端异常处理

场景：

- Python 进程启动失败
- 端口被占用
- 后端运行中退出
- `/health` 超时

处理：

- 顶部状态栏显示“后端离线”
- 设置页显示详细错误
- 提供“重新启动后端”按钮
- 失败日志写入本地日志文件

## 10. 状态管理设计

### 10.1 Config Store

```ts
interface ApiConfig {
  apiBaseUrl: string;
  useServerKey: boolean;
  apiKeyConfigured: boolean;
  proxyMode: "none" | "system" | "http" | "socks5";
  proxyUrl?: string;
}
```

### 10.2 Task Store

```ts
interface TaskState {
  tasks: Record<string, ImageTask>;
  activeTaskIds: string[];
  addTask(task: ImageTask): void;
  updateTask(taskId: string, patch: Partial<ImageTask>): void;
  retryTask(taskId: string): Promise<void>;
  cancelTask(taskId: string): Promise<void>;
}
```

### 10.3 Gallery Store

```ts
interface GalleryState {
  items: GalleryItem[];
  filters: GalleryFilters;
  addFromTask(task: ImageTask): Promise<void>;
  removeItem(id: string): Promise<void>;
  exportItem(id: string): Promise<void>;
}
```

## 11. 数据持久化

### 11.1 配置

存储内容：

- API Base URL
- 是否使用服务端默认 Key
- 代理设置
- 最近使用模型
- UI 偏好

不直接存储明文 API Key。

### 11.2 密钥

优先级：

1. 系统钥匙串：`keytar`
2. 本地加密配置：`electron-store`
3. 仅本次会话内存保存

### 11.3 画廊

建议目录：

```text
~/Pictures/GeneratePlaylistStudio/
  images/
  thumbnails/
  metadata.sqlite
```

图片：

- 原图保存到 `images`
- 缩略图保存到 `thumbnails`
- 参数保存到 `metadata.sqlite`

## 12. 用户流程

### 12.1 首次启动

```text
启动 App
  |
  v
自动启动本地后端
  |
  v
检测 server_key_configured
  |
  v
如果服务端有 Key：默认勾选“使用服务端默认 Key”
如果服务端无 Key：引导用户填写本地 Key
  |
  v
进入文生图页
```

### 12.2 文生图

```text
输入提示词
  |
  v
选择模型和参数
  |
  v
点击生成
  |
  v
提交异步任务
  |
  v
任务卡片进入队列
  |
  v
轮询任务状态
  |
  v
生成完成
  |
  v
加入画廊并展示结果
```

### 12.3 图生图

```text
拖入图片
  |
  v
填写提示词
  |
  v
设置重绘强度
  |
  v
提交 /api/images/edits
  |
  v
轮询任务
  |
  v
结果进入画廊
```

## 13. 开发计划

### 13.1 MVP

目标：桌面端能完整跑通文生图。

范围：

- Electron 基础窗口
- React + Tailwind 深色主题
- 自动连接本地 FastAPI 后端
- API 配置面板
- 使用服务端默认 Key
- 文生图
- 任务提交和轮询
- 简单任务卡片
- 简单画廊
- 保存图片到本地

不做：

- ControlNet
- 插件系统
- 复杂图片编辑器
- 社区模板
- 真实价格计费

### 13.2 V1.0

目标：变成可日常使用的桌面工具。

范围：

- 图生图
- 任务取消 / 重试
- 参数预设
- 提示词历史
- 提示词收藏
- 画廊筛选
- 元数据查看
- 代理配置
- 系统通知

### 13.3 V1.5

目标：专业生图工作流。

范围：

- 批量生成
- CSV 导入
- ControlNet
- 局部重绘
- 插件后端
- 多后端节点
- 模板市场

## 14. 验收标准

### 14.1 MVP 验收

必须满足：

- App 可以启动桌面窗口。
- App 可以启动或连接 FastAPI 后端。
- `/health` 能正确反馈在线状态。
- 用户可以配置 API Base URL。
- 当后端有 `UPSTREAM_API_KEY` 时，前端可以不填 Key 生成图片。
- 用户可以提交文生图任务。
- 任务提交后立即显示在任务队列。
- 任务状态可以自动轮询更新。
- 任务成功后图片显示在主界面。
- 生成结果可以进入画廊。
- 用户可以保存图片到本地。
- 上游生图超过 300s 时，前端不会因为单次请求断开而丢任务。

### 14.2 V1.0 验收

必须满足：

- 支持图生图。
- 支持任务失败后重试。
- 支持本地历史筛选。
- 支持复制提示词。
- 支持查看生成参数。
- 支持代理配置。
- 支持系统通知。

## 15. 风险与处理

### 15.1 Python 后端打包复杂

风险：

- Python 依赖和平台差异导致打包体积变大。

处理：

- MVP 先使用开发模式启动 Python。
- 正式版再把后端打为 sidecar。
- 后端依赖保持克制，避免引入大型运行时。

### 15.2 API Key 安全

风险：

- Key 被写入日志、任务记录或前端状态。

处理：

- 优先使用服务端环境变量。
- 本地 Key 只存在钥匙串或加密存储。
- 日志中屏蔽 Authorization。
- 任务元数据不保存 Key。

### 15.3 进度不准确

风险：

- 上游没有真实进度，只能显示 pending/running。

处理：

- MVP 显示“处理中”和等待时间。
- 如果后端支持真实进度，再展示百分比。
- 不伪造精确进度，只做弱提示。

### 15.4 取消任务不彻底

风险：

- 上游不支持取消，前端取消只是停止轮询。

处理：

- MVP 标注为“停止等待”。
- V1 后端新增 cancel 状态。
- 如果上游支持取消，再透传取消请求。

## 16. 待补充接口

为了让桌面端体验完整，建议后端下一步补充：

1. `GET /api/capabilities`
2. `GET /api/models`
3. `POST /api/preflight`
4. `POST /api/moderation/check`
5. `POST /api/tasks/{taskId}/cancel`

其中 MVP 最优先：

1. `GET /api/capabilities`
2. `POST /api/preflight`

这两个接口能让桌面端正确判断“是否使用服务端默认 Key”和“连接测试是否成功”。

## 17. 最终建议

第一阶段不要直接做全量功能，建议按这个顺序推进：

1. 新建 Electron + React + TypeScript 项目。
2. 把当前 `index.html` 的核心提示词组装逻辑迁移成 React hooks。
3. Electron 主进程启动现有 FastAPI 后端。
4. React 接入 `/api/images/generations` 和 `/api/tasks/{taskId}`。
5. 做任务队列和画廊最小闭环。
6. 再补图生图、代理、预设、历史筛选。

这个顺序能最快验证桌面端主链路，并且最大程度复用当前项目已经完成的异步后端能力。
